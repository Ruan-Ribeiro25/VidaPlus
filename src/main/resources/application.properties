server.port=8080

# --- BANCO DE DADOS (ATUALIZADO PARA VIDAPLUS) ---
spring.datasource.url=jdbc:mysql://localhost:3306/vidaplus_db?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=root
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# --- HIBERNATE (MODO SEGURO) ---
# AGORA ESTÁ COMO 'update'. 
# Isso mantém seus dados salvos e apenas atualiza a estrutura se necessário.
spring.jpa.hibernate.ddl-auto=update

spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# --- THYMELEAF ---
spring.thymeleaf.cache=false
spring.thymeleaf.prefix=classpath:/templates/
spring.thymeleaf.suffix=.html
spring.thymeleaf.check-template-location=true

# --- UPLOAD DE ARQUIVOS ---
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# --- EMAIL ---
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=vidapluscontato@gmail.com
spring.mail.password=ctxs aeqh qirl gfav
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
